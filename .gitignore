# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Next.js ──
.next/
out/

# ── Build ──
build/
dist/

# ── Environment (NEVER commit secrets) ──
# ALL env files stay off git (incl. .env.example) — shared privately within
# the team, never committed. Team policy: no `!.env.example` exception.
.env
.env.*
thequantsoft/.env
thequantsoft/.env.*

# ── SSL / Credentials ──
*.pem
*.key
certbot/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Debug ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── TypeScript ──
*.tsbuildinfo
next-env.d.ts

# ── Docker ──
docker-compose.override.yml

# ── Vercel ──
.vercel

# ── graphify (generated knowledge-graph artifacts; rebuild with `graphify update .`) ──
graphify-out/

# ── Per-machine Claude Code config (each teammate installs locally) ──
CLAUDE.md
.claude/

